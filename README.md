# Static Website Server Generator in Zig
Put your desired repo in config.zon and the build step clones the repo and turns the site into a single binary containing all the files and hosts then
requires at least a index.html and a 404.html
